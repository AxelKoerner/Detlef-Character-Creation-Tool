import {render} from '@testing-library/react'
import CustomLineage from "../scenes/Custom/CustomLineage";

describe('Custom Lineage', () => {
    test('First Test', () => {
        render(<CustomLineage/>);

    })
})